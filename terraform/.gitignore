# Downloaded provider plugins — regenerated by terraform init
.terraform/

# Terraform state files — contain sensitive data, never commit these
*.tfstate
*.tfstate.backup

# Terraform lock file for providers (optional but recommended to keep)
# .terraform.lock.hcl

